name: Publish packages

on:
  push:
    tags: ["*"]

permissions:
  contents: write
  id-token: write
  attestations: write

jobs:
  github:
    uses: tree-sitter/workflows/.github/workflows/release.yml@main
    with:
      generate: true
      attestations: true
  npm:
    name: Publish npm package
    runs-on: ubuntu-latest
    environment:
      name: npm
      url: https://www.npmjs.com/package/tree-sitter-mulle-objc
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Set up tree-sitter
        uses: tree-sitter/setup-action/cli@v2
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{vars.NODE_VERSION || '22'}}
          registry-url: https://registry.npmjs.org
      - name: Install dependencies
        run: npm install
      - name: Generate parser
        run: tree-sitter generate
      - name: Configure npm OIDC authentication
        run: npm config set //registry.npmjs.org/:_auth-type=oidc
      - name: Publish to npm
        run: npm publish --provenance --access public
# MEMO: cargo auth redirector broken, could not get a token
#  crates:
#    uses: tree-sitter/workflows/.github/workflows/package-crates.yml@main
#    secrets:
#      CARGO_REGISTRY_TOKEN: ${{secrets.CARGO_REGISTRY_TOKEN}}
#    with:
#      generate: true
  pypi:
    uses: tree-sitter/workflows/.github/workflows/package-pypi.yml@main
    secrets:
      PYPI_API_TOKEN: ${{secrets.PYPI_API_TOKEN}}
    with:
      generate: true
